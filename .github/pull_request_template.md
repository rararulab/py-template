## Summary

<!-- 1-3 sentences. What changed and why. -->

## Type of change

<!-- Pick one and add the matching label to this PR. -->

| Type | Label |
|------|-------|
| Bug fix | `bug` |
| New feature | `enhancement` |
| Breaking change | `breaking-change` |
| Refactor | `refactor` |
| CI / Infrastructure | `ci` |
| Maintenance | `chore` |
| Documentation | `documentation` |

## Component

<!-- Add the component label that matches the area you changed. -->
<!-- `src` · `cli` · `tests` · `ci` · `docs` · `build` · `other` -->

## Closes

<!-- REQUIRED. PR merge auto-closes the linked issue. -->
<!-- If no issue exists, create one first: `gh issue create`. -->

Closes #

## Issue context used

<!-- Agent-authored PRs MUST list the issue context consumed before editing.
     This is the audit trail: it lets a reviewer (or a future agent) confirm
     the implementation matches the issue's Agent Task Packet. -->

- Issue:
- Agent Task Packet source:
- Issue comments that changed the plan:

## Deviations from issue

<!-- Every scope or design change discovered during implementation. -->
<!-- Use "None." if unchanged. -->

## Agent notes

<!-- Reviewer-relevant notes, known trade-offs, or follow-up issue numbers. -->
<!-- Use "None." if not needed. -->

## Test plan

- [ ] `just check` passes
- [ ] `just test` passes
- [ ] Tested locally where automation can't cover it

## Verification evidence

<!-- Paste the exact local commands you ran and the CI check summary
     (`gh pr checks <PR>`) before reporting done. Evidence before assertions. -->
